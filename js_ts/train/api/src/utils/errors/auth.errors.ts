export enum AuthErrors {
  EMAIL_ALREADY_EXISTS = 'AuthError: User with this email already exists. In case you forgot your password please use the "Forgot password" option.',
  USER_NOT_FOUND = 'AuthError: User not found',
  INVALID_TOKEN = 'AuthError: Invalid token',
}
